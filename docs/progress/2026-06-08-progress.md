# 操作進度紀錄 — 2026-06-08

## 本日工作摘要

1. 全面 ADR 合規檢查（詳見 `2026-06-08-adr-compliance.md`）
2. 修正 `prepare_taco.py` 路徑 fallback bug
3. 修正 `train_colab.ipynb` Cell 6（TACO 下載 + 合併流程）
4. 推上 GitHub

---

## 程式碼修正

### `data/prepare_taco.py` — 路徑 fallback 修正

**問題**：TACO repo 直接 clone 後，`annotations.json` 位於 `data/` 根目錄，
而非 `annotations/annotations.json` 子目錄，導致 `FileNotFoundError`。

**修正**：新增 fallback，先找 `annotations/annotations.json`，
若不存在再找 `annotations.json`（直接 clone 情況）。

### `train_colab.ipynb` Cell 6 — TACO 下載流程修正

**問題**：
- `cd` 路徑錯誤，未回到 `/content/AI-course`
- `--taco-dir` 未指向正確的資料根目錄
- 缺少下載後的結構確認步驟

**修正**：
- 明確 `%cd /content/AI-course/data/TACO_repo` 再執行 downloader
- 執行後 `%cd /content/AI-course` 回到專案根
- 加入 `os.listdir()` 確認 TACO 結構
- `--taco-dir data/TACO_repo/data`（指向含 annotations.json 的目錄）

---

## Colab 執行狀態

| Cell | 狀態 | 備註 |
|------|------|------|
| Cell 1 GPU 確認 | ✅ Tesla T4 | |
| Cell 2 安裝套件 | ✅ | ultralytics + kaggle |
| Cell 3 Clone repo | ✅ | |
| Cell 4A 下載資料集 | ✅ | garbage-classification 82MB |
| Cell 5 TrashNet 轉換 | ✅ | 2527 張，Class4=0 |
| Cell 6 TACO 下載+合併 | 🔄 待執行 | 預計 20-40 分鐘 |
| Cell 7 訓練 | ❌ 待執行 | |
| Cell 8 下載 best.pt | ❌ 待執行 | |

---

## 下一步行動

1. 在 Colab 執行 Cell 6（TACO 下載，約 20-40 分鐘）
2. Cell 7 的 `DATA_YAML` 改為 `data/merged/data.yaml` 再執行訓練
3. Cell 8 下載 best.pt
4. SCP 傳至 Jetson：`scp best.pt jetson@<IP>:~/AI-course/models/`
5. Jetson 上執行 TensorRT 轉換：`python src/main.py --export-trt`
6. 接硬體（LED × 6、蜂鳴器），執行 `python src/main.py --no-display`
